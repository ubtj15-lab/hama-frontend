{
  "name": "hama-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:hama": "npm run dev --workspace=apps/hama",
    "dev:partner": "npm run dev --workspace=apps/partner",
    "build:hama": "npm run build --workspace=apps/hama",
    "build:partner": "npm run build --workspace=apps/partner"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
